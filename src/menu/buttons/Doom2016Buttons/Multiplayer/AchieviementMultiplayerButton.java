package menu.buttons.Doom2016Buttons.Multiplayer;

import components.Button;
import javafx.geometry.Pos;
import javafx.scene.paint.Color;

public class AchieviementMultiplayerButton extends Button {
    public AchieviementMultiplayerButton(int x, int y, String text, int width, int height, String imagePath, String imagePath2, String moviePath, String fontPath, double fontSize, Color textColor, Color textColor2, Pos alignment) {
        super(x, y, text, width, height, imagePath, imagePath2, moviePath, fontPath, fontSize, textColor, textColor2, alignment);
    }
}
